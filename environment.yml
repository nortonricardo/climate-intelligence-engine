name: climate-engine
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python=3.11

  # Dados e análise
  - numpy
  - pandas
  - scipy

  # Machine Learning
  - scikit-learn        # regressão linear, KNN, random forest
  - xgboost             # gradient boosting (útil para dados climáticos)
  - lightgbm

  # Deep Learning (redes densas + transformers)
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::torchaudio
  - pytorch::pytorch-cuda=12.1  # ajuste para a versão CUDA da máquina (nvidia-smi)

  # GPU — aceleração numpy (CuPy)
  # Instale após criar o ambiente: pip install cupy-cuda12x
  # Para CUDA 11.x: pip install cupy-cuda11x
  # Verifique a versão: nvidia-smi | grep "CUDA Version"

  # Dados climáticos / geoespaciais
  - xarray              # dados multidimensionais (NetCDF, GRIB)
  - netcdf4
  - cfgrib
  - cartopy             # mapas e projeções
  - geopandas
  - rioxarray

  # Visualização
  - matplotlib
  - seaborn
  - plotly

  # Notebooks e dev
  - jupyter
  - ipykernel
  - tqdm

  # Utilitários
  - pip
  - pip:
    - transformers       # HuggingFace Transformers (BERT, TimeSeries, etc.)
    - datasets
    - evaluate
    - mlflow             # tracking de experimentos
    - hydra-core         # gestão de configs
    - gdown              # download de arquivos do Google Drive
    - weasyprint         # exportação de notebooks para PDF
